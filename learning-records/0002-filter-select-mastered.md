# filter + select cold-write demonstrated

User completed all 8 runnable drills in `exercises/01_filter_select.py` (PASS). Comfortable with `col`, `&`/`|`/`!=`, `isin`, range predicates, and `alias` on select.

Implications: next ZPD is column shape changes — `withColumn`, rename, drop, cast, simple `when`/`otherwise`.
