# Joins drills passed; fluency still weak

User completed 03_joins 8/8 but reported they cannot yet use 0001–0003 fluently in combination. Prefer integration pipelines over new API until chains feel automatic.

Implications: ship `04_pipeline_mix` before groupBy; treat composition as its own practice layer.
