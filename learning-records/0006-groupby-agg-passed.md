# groupBy + agg 8/8

User completed `05_groupby_agg` with all cases green: count/sum/avg, multi-agg + alias, multi-key, pre-agg filter, post-agg filter (HAVING), join-then-groupBy.

Implications: mission core DF chain (filter → join → groupBy → agg) is complete. Optional next: window functions, or cold integration mix of full chain.
